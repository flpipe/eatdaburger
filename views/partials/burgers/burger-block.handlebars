<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Burger TIME!{{else}}time to eat burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
